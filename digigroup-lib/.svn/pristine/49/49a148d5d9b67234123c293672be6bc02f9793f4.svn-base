package mindmaps.map {
	import mx.collections.ArrayCollection;

	import vos.*;

	public class MapContextFactory {
		public function createDefault():MapContext {
			var elementTypes:Array = [ Item, Note, Task, Contact, Appointment, Iteration ];
			var res:MapContext = new MapContext(elementTypes);
			return res;
		}
	}
}
